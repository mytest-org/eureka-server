@Library('jenkins-shared-library_git') _

pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                ExecuteGradle ('./gradlew clean build')
            }
        }

    }
}
