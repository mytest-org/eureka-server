@Library('jenkins_shared_libraries_git') _

pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                //Load gradle command and arguments
                load "config/gradle.groovy"
                ExecuteGradle ('./gradlew clean build')
            }
        }

    }
}
